.env
.venv/
__pycache__/
.pytest_cache/
*.pyc
alert_history.jsonl
trading_history.jsonl
*.db
*.sqlite
*.sqlite3

# Accidental shell-redirect artifacts (e.g. an unquoted `pip install pkg >=x.y`
# can write a file literally named `=x.y`)
=*

# Local scratch logs and ad-hoc run reports
*.log
run_log.txt
pcr.txt
pytest_output.txt
pytest_run.txt
pytest_clean.txt

# Trained RL model archives (regenerated by training)
*.zip
model_registry/
